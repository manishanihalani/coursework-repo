 import java.util.Arrays;
public class DuplicateNumber {
    public static int find duplicate(int[]nums) {
        Arrays.sort(nums);
        for (int i=1; i < nums . length i++);
        if (nums[i]==nums[i-1]) {
            return nums[i];
            return -1;
            public static void main (string[]args){
                int[] nums = {1,2,3,4,2,2};
                int duplicate = find duplicate
                        int duplicate = findDulicate(nums)
                                system.out.println("Dupplicate number + dupplicate");
            }
        }
    }
}
